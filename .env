# OpenProject MCP Server Configuration
# 
# This file contains all the configuration settings needed to connect the MCP Server
# to your OpenProject instance. Replace the placeholder values with your actual settings.

# ============================================================================
# REQUIRED SETTINGS
# ============================================================================

# OpenProject Instance URL
# The URL where your OpenProject instance is running.
# Must include the protocol (http:// or https://) and port if not standard.
# Examples:
#   - Local development: http://localhost:8080
#   - Docker deployment: http://localhost:8080
#   - Cloud instance: https://yourcompany.openproject.com
OPENPROJECT_URL=http://openproject-web-1:8080

# OpenProject Host Header (optional)
# If your OpenProject instance is behind a proxy that requires a specific Host header,
# set it here. Otherwise, the host will be extracted from OPENPROJECT_URL.
# Example: OPENPROJECT_HOST=openproject.dvvcloud.work
OPENPROJECT_HOST=openproject.dvvcloud.work

# OpenProject API Key
# Your personal API key for authentication with OpenProject.
# How to get your API key:
#   1. Login to your OpenProject instance
#   2. Go to "My Account" → "Access Tokens" (or "API Access Key")
#   3. Click "New token" or "Generate new token"
#   4. Enter a name (e.g., "MCP Server")
#   5. Copy the 40-character token and paste it below
# IMPORTANT: Keep this key secure and never commit it to version control
OPENPROJECT_API_KEY=589f132dd54c1e8af6f85273049af282f57d86bb906c7d371d5646788dbd322b

# ============================================================================
# MCP SERVER CONFIGURATION
# ============================================================================

# MCP Server Host
# The host address the MCP server will bind to.
# Use 0.0.0.0 for Docker containers to accept connections from any IP
# Use localhost for local development
MCP_HOST=0.0.0.0

# MCP Server Port
# The port the MCP server will listen on.
# IMPORTANT: This must match the port configured in your Claude Desktop settings
# Default: 39127 (standard MCP port)
MCP_PORT=39127

# MCP Log Level
# Controls the verbosity of server logs.
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
MCP_LOG_LEVEL=INFO

# ============================================================================
# OPTIONAL PERFORMANCE SETTINGS
# ============================================================================

# Cache Timeout (in minutes)
# How long to cache OpenProject configuration data (types, statuses, priorities)
# Default: 300 (5 minutes)
CACHE_TIMEOUT=300

# Pagination Size
# Number of items to fetch per page when listing large datasets
# Default: 100
PAGINATION_SIZE=100

# Maximum Retries
# Number of times to retry failed API requests
# Default: 3
MAX_RETRIES=3

# ============================================================================
# TROUBLESHOOTING
# ============================================================================

# If you encounter issues:
# 1. Verify OPENPROJECT_URL is accessible in your browser
# 2. Check that OPENPROJECT_API_KEY is correct (40 characters)
# 3. Ensure the API key has project creation and work package permissions
# 4. Check that the MCP_PORT matches your Claude Desktop configuration
# 5. Review server logs for detailed error messages
